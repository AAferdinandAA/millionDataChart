# SciChart Vue Demo - 百万级数据点图表渲染

一个基于 Vue 3 + TypeScript + Vite 的高性能图表展示项目，专门用于演示大规模数据集（百万级数据点）的可视化渲染。项目对比展示了 ECharts 和 SciChart 两种图表库在处理大数据量时的性能表现。

## 📋 项目特性

- **高性能渲染**：使用 Web Worker 处理数据采样，支持百万级数据点的流畅展示
- **双引擎对比**：集成 ECharts 和 SciChart 两个主流图表库
- **Vue 3 + TypeScript**：采用最新的 Vue 3 Composition API 和 TypeScript 进行开发
- **响应式设计**：完全响应式的图表容器，支持窗口自适应
- **数据采样优化**：使用 Web Worker 进行后台数据处理，不阻塞主线程

## 🛠️ 技术栈

- **框架**：Vue 3
- **语言**：TypeScript
- **构建工具**：Vite
- **图表库**：
  - ECharts 6.x（LineChart 组件）
  - SciChart 4.x（MillionPointsChart 组件）
- **并发处理**：Web Worker

## 📦 项目结构

```
src/
├── components/
│   ├── LineChart.vue              # ECharts 图表组件（支持百万数据点）
│   └── MillionPointsChart.vue      # SciChart 图表组件（原生高性能渲染）
├── works/
│   └── dataWorker.ts              # Web Worker 数据采样脚本
├── App.vue                        # 应用主组件
├── main.ts                        # 应用入口
└── style.css                      # 全局样式
```

## 🚀 快速开始

### 环境要求

- Node.js >= 16.0
- npm >= 8.0

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

启动后访问 `http://localhost:5173` 查看应用。

### 构建生产版本

```bash
npm run build
```

### 预览构建结果

```bash
npm run preview
```

## 📊 组件说明

### LineChart 组件

基于 ECharts 的线图组件，支持大数据量渲染：

```vue
<LineChart :dataSize="5000000" // 数据点总数 :samplePoints="5000" //
采样数据点数 />
```

**特性**：

- Web Worker 数据采样，避免主线程阻塞
- 支持自定义数据源 URL
- 响应式缩放与平移
- 性能监控（加载、渲染耗时）

### MillionPointsChart 组件

基于 SciChart 的原生高性能图表组件：

```vue
<MillionPointsChart />
```

**特性**：

- GPU 加速渲染
- 原生支持百万级数据点
- 流畅的交互体验
- 实时性能指标显示

## 💡 核心实现

### Web Worker 数据采样

在 `src/works/dataWorker.ts` 中实现的数据采样算法：

```typescript
// 将原始数据采样为指定数量的点
// 保留关键数据特征，提高渲染性能
```

### 响应式容器

图表容器采用 `100vh` 和 `100vw` 设置，自动适应视口大小。

## 🔧 配置

### 数据量配置

在 `src/components/LineChart.vue` 中修改默认配置：

```typescript
const props = withDefaults(
  defineProps<{
    dataUrl?: string; // 自定义数据源 URL
    dataSize?: number; // 数据点总数，默认 5,000,000
    samplePoints?: number; // 采样点数，默认 5,000
  }>(),
  {
    dataSize: 5_000_000,
    samplePoints: 5_000,
  }
);
```

## 📈 性能指标

- **数据加载**：使用 Web Worker 后台处理，不阻塞 UI
- **渲染性能**：支持 5-10 百万数据点实时渲染
- **内存占用**：采样算法有效降低内存消耗

## 📚 相关资源

- [Vue 3 文档](https://vuejs.org/)
- [Vue 3 TypeScript 指南](https://vuejs.org/guide/typescript/overview.html#project-setup)
- [Vite 文档](https://vitejs.dev/)
- [ECharts 文档](https://echarts.apache.org/)
- [SciChart 文档](https://www.scichart.com/javascript-chart-library/)

## 📝 许可证

MIT
